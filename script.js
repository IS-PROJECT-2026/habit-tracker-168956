// Habit Tracker — app logic will be added in upcoming issues
const habitList = document.getElementById("habit-list");
const emptyState = document.getElementById("empty-state");
emptyState.hidden = false;
